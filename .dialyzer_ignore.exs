# Dialyzer warning filters.
#
# Empty by design: slice 01 establishes the baseline without suppressing anything,
# so the true warning count is visible in FINDINGS.md. Entries added here must cite
# a reason and a slice number -- never add one to make a gate pass.
[]
